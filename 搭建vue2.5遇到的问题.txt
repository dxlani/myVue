1.路由history模式需要服务器配置支持 ，暂用hash模式
2.webpack配置默认static为静态资源，并且为绝对路径，assets里面放静态资源，没有被打包，暂时webpack里面配了assets的拷贝路径
3.页面使用html，ts中引入的scss样式文件，可以改进成vue替代html,在vue文件中使用style引入，并scoped,控制当前模块作用域，但是这个方法报错找不到此模块
4.axios处理序列化参数的时候对时间也进行了编码，后台没有对此进行解码，暂用url里面拼接参数的方法，有待优化
5.VeeValidate校验器无法中文化，可以通过自定义正则规则和提示信息来处理